/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    qualities: [75, 85],
  },
  allowedDevOrigins: ['192.168.100.71'],
};

export default nextConfig;
